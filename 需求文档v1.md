# Doc-Helper 产品需求描述文档

> 版本：v2（由需求文档 v1 细化）｜ 状态：草稿
> 已定决策：Q1 自用 / Q2 自建聊天界面 / Q3 脚本规范 / Q4 MVP 工具组 / Q5 内置二进制 / Q12 强制内置 / Q13 拆分两个合并工具
> 未确认决策（按推荐值暂填）：Q6–Q11，见文末"待确认事项"

## 1. 产品概述

一句话：Doc-Helper 是一个自用的文档处理助手，通过一个本地聊天界面，让用户用自然语言完成 xlsx / docx / pptx 文档的常见处理（合并、拆分、格式调整、数据清洗、批量替换等）。

- 目标用户：用户本人（自用）；长期目标可扩展到 Office/WPS 小白
- 核心价值：把"文档处理"变成"说一句话就完成"，不必记住复杂操作步骤
- 定位：自用工具，本期不面向市场分发

## 2. 目标 / 非目标

### 目标（MVP）
- 完成 7 个 MVP 工具（见 §8）
- 可用的本地聊天界面，支持 function calling 工具调用
- agent 能添加 / 修改 / 产出工具（草稿确认制）

### 非目标（本期不做）
- 多用户、账号体系
- 移动端
- 云部署 / 远程访问
- 商业化分发、安装包制作
- pptx 处理（本期只做 xlsx 与 docx）

## 3. 系统架构（三层）

```
┌────────────────────────────────────────────┐
│ 顶层  智能体（本地聊天界面）                    │
│   Node + Express + DeepSeek(OpenAI 兼容)     │
│   自然语言 → 工具调用 → 回答                  │
├────────────────────────────────────────────┤
│ 中间层  工具层 tools/                        │
│   一组 PowerShell 脚本 + meta.json           │
│   每个工具 = 一段脚本 + 元数据                │
├────────────────────────────────────────────┤
│ 底层  officecli（内置 vendor/）              │
│   officecli.exe 处理 .docx/.xlsx/.pptx      │
└────────────────────────────────────────────┘
```

### 3.1 顶层：智能体
- 形态：本地 Web 页（浏览器打开 localhost 即用）
- 技术栈：Node + Express + OpenAI 兼容 SDK
- LLM：DeepSeek（deepseek-chat）
- 能力：
  1. 识别用户意图 → 选择工具 → 执行 → 返回结果
  2. 用户要求"添加 / 修改 / 产出工具"时，生成工具脚本并落盘为草稿
  3. 利用 officecli 的 schema 帮助回答文档处理相关问题

### 3.2 中间层：工具层
- 一个工具 = `tools/<工具名>/` 目录，含 `tool.ps1` + `meta.json`
- 工具脚本用 PowerShell 编写，内部调用 officecli 完成文档处理；其余逻辑（遍历文件、复制、重命名等）用 PowerShell 完成
- 工具规范见 §5

### 3.3 底层：officecli
- 内置 `vendor/officecli/officecli.exe`，**强制使用内置**，不依赖系统安装（Q5-C、Q12-B）
- 后端启动时校验内置二进制存在且可执行
- 工具脚本统一通过环境变量 `OFFICECLI` 获取 officecli 路径（由后端执行工具时注入）
- officecli 核心能力参考（供工具实现）：
  - 文档操作：`create / add / set / get / query / remove / move / swap / batch / dump / import / merge / raw / raw-set / save / close / validate`
  - 批处理：`batch`（一次 JSON 数组多命令，配合 resident 常驻进程）
  - 能力参考：`officecli help <format> <verb> <element>`（schema 化输出）

## 4. 目录结构

```
Doc-Helper/
├── package.json                # Node 后端依赖与启动脚本
├── config.json                 # 端口、LLM 模型等（API key 不入库）
├── server/
│   ├── index.js                # Express 入口 + 静态托管 web/
│   ├── agent.js                # LLM 调用 + function calling 循环
│   ├── toolRunner.js           # 扫描 tools/、执行 tool.ps1、超时/取消
│   └── config.js               # 配置加载
├── web/
│   └── index.html              # 聊天页（含工具草稿启用入口）
├── tools/
│   ├── xlsx-merge-sheets/
│   ├── xlsx-append-data/
│   ├── xlsx-split/
│   ├── xlsx-format/
│   ├── xlsx-clean/
│   ├── docx-replace/
│   └── docx-merge/
└── vendor/
    └── officecli/officecli.exe
```

## 5. 工具规范

### 5.1 目录与文件
- `tools/<name>/tool.ps1`：主脚本，入口通过 PowerShell 命名参数接收输入
- `tools/<name>/meta.json`：元数据（含 `enabled` 草稿标记）

### 5.2 tool.ps1 输入输出约定
- **输入**：命名参数（`param` 块声明）；路径一律用绝对路径，允许包含空格
- **成功**：stdout 只输出一个 JSON：`{"ok":true,"outputFiles":[...],"summary":"..."}`
- **失败**：stderr 输出错误信息，退出码非 0；可选 stdout JSON `{"ok":false,"error":"..."}`
- **日志**：`Write-Host` / 写日志文件，不污染 stdout
- **编码**：脚本以 UTF-8 保存；文件路径与内容处理避免编码问题
- **输出路径**：由调用方（agent / 用户）显式传入；未传时默认写到输入文件同目录下带 `_out` 后缀

### 5.3 meta.json 结构（OpenAPI 风格，可直接映射 function calling）

```json
{
  "name": "xlsx-merge-sheets",
  "version": "1.0.0",
  "enabled": true,
  "description": "把多个 xlsx 文件合并为一个工作簿，每个源文件对应一个工作表",
  "parameters": {
    "type": "object",
    "properties": {
      "inputDir": { "type": "string", "description": "包含待合并 xlsx 的目录" },
      "output":   { "type": "string", "description": "输出 xlsx 文件路径" }
    },
    "required": ["inputDir", "output"]
  },
  "output": "合并后的文件路径与 sheet 清单"
}
```

### 5.4 工具生命周期
- **草稿**：agent 生成后 `enabled=false`，不注册到 LLM，前端提示启用
- **启用**：用户在界面点击启用 → `enabled=true` → 下次扫描注册进 function calling
- **修改**：agent 修改现有工具时，先备份原文件到 `tools/<name>/.backup/`，改动仍先落为草稿，确认后生效
- **删除**：需用户确认

## 6. 聊天界面功能

- 会话窗口：消息流式展示（SSE）
- 工具调用过程可视化：展示"正在执行工具 xlsx-merge-sheets（参数…）"
- 工具草稿通知：agent 产出新工具后显示"启用"按钮
- 配置：API key / 模型 / 端口（本期从 `config.json` 修改，界面配置可后置）

## 7. 智能体流程（后端）

### 7.1 工具调用主循环
1. 启动时扫描 `tools/*/meta.json`（`enabled=true`）→ 生成 function schema
2. system prompt 注入：产品角色、工具使用说明、officecli 帮助要点、安全约束
3. 用户消息 → DeepSeek → 若返回 `tool_call`：校验参数 → 执行工具
4. 执行：`powershell -NoProfile -File tool.ps1 <参数>`，注入 `OFFICECLI` 环境变量，超时 120s，可取消
5. 解析 JSON → 作为 tool result 回传 → LLM 生成最终回答 → SSE 推送

> **安全红线**：后端只按工具名查表执行 `tools/` 下已启用工具，绝不把 LLM 返回的任意字符串当命令执行。

### 7.2 产出 / 修改工具流程
1. 用户："帮我做个把多个 word 合并的工具"
2. LLM 生成 `tool.ps1` + `meta.json` 内容
3. 后端校验（文件名安全、无路径穿越）→ 写入 `tools/<name>/` → `enabled=false`
4. 前端提示"新工具已生成（草稿），点此启用"
5. 用户启用 → 生效

## 8. MVP 工具集（7 个）

### 8.1 xlsx-merge-sheets（多表合并）
- 用途：把多个 xlsx 合并为一个工作簿，每个源文件一个 sheet
- 输入：`inputDir`（含待合并 xlsx 的目录）、`output`（输出路径）
- 行为：对目录下每个 `.xlsx` 新建一个 sheet（sheet 名 = 源文件名），复制全部单元格内容；输出已存在时提示
- 输出：单个 xlsx + 汇总 JSON（sheet 清单）

### 8.2 xlsx-append-data（同结构表纵向拼接）
- 用途：把多个同结构 xlsx 纵向拼接成一张表
- 输入：`inputFiles`（文件数组）或 `inputDir`、`output`
- 行为：假定各文件首行表头一致，纵向拼接；可选去重
- **待细化**：表头不一致时的处理策略（报错 / 按列名对齐 / 跳过）

### 8.3 xlsx-split（拆分工作簿）
- 用途：把一个工作簿拆成多个独立文件
- 输入：`input`（xlsx 文件）、`outputDir`
- 行为：每个 sheet 保存为一个独立 xlsx（文件名 = sheet 名）

### 8.4 xlsx-format（批量格式调整）
- 用途：对 xlsx 做批量格式调整
- 输入：`inputDir` 或 `inputFiles`、格式规则（参数传入）、`outputDir`
- **待细化**：支持哪些格式（列宽、字体、对齐、数字格式、表头底色/加粗、冻结首行等），作用范围（整个文件 / 指定 sheet）

### 8.5 xlsx-clean（数据清洗）
- 用途：对表格数据做清理
- 输入：`input` 或 `inputDir`、清洗选项、`output`
- **待细化**：去重、去空行、去首尾空格、统一大小写、填充空值、类型转换等各选项

### 8.6 docx-replace（批量替换关键词）
- 用途：批量替换 docx 中的关键词
- 输入：`inputDir`（docx 文件）、替换映射（JSON 文件，`{"旧词":"新词"}`）、`outputDir`
- 行为：对每个 docx 按映射替换，输出新文件（不改原文件）
- **待细化**：替换范围（正文 / 页眉页脚 / 表格）、大小写敏感、词边界

### 8.7 docx-merge（合并文档）
- 用途：把多个 docx 合并成一个
- 输入：`inputFiles`、`output`
- 行为：按顺序合并，默认每个文档另起一页
- **待细化**：是否保留各文档样式 / 页眉页脚，冲突时如何处理

## 9. 非功能需求

- 平台：Windows；PowerShell 5.1；Node 18+（当前环境 Node 26）
- 单机 localhost 服务，默认端口 3000（可配）
- 错误处理：工具失败返回可读中文错误；LLM 调用失败可重试 / 提示
- 日志：`server/logs/` 记录调用与工具执行日志
- 安全：草稿确认制；只执行注册工具；路径安全校验；API key 存 `config.json`（gitignore）或环境变量
- 性能：大文件（几十 MB）处理可接受秒级延迟；SSE 流式缓解等待感

## 10. 待确认事项（open questions）

| # | 问题 | 当前假设（推荐值） |
|---|------|-------------------|
| Q6 | LLM 提供商 | DeepSeek（OpenAI 兼容、function calling）；是否有现成 API key？ |
| Q7 | 交互形态 | 本地 Web 页；是否想先做终端 CLI 快速验证？ |
| Q8 | 后端技术栈 | Node + Express；是否倾向 Python FastAPI？ |
| Q9 | 执行机制 | 超时 120s + 可取消 |
| Q10 | 安全边界 | 草稿确认制（agent 产出的工具需手动启用） |
| Q11 | meta.json 结构 | §5.3 所示结构 |
| Q15 | 工具行为细节 | §8 中标注"待细化"的各项 |

## 11. 里程碑

- M1：框架跑通 —— 目录结构、工具规范、后端 function calling 骨架、1 个示例工具（xlsx-merge-sheets）
- M2：补齐 7 个 MVP 工具
- M3：agent 产出 / 修改工具流程（草稿确认制）
- M4：打磨（流式展示、错误提示、日志）
