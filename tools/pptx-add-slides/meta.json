{
  "name": "pptx-add-slides",
  "title": "批量新建幻灯片",
  "version": "1.0.0",
  "enabled": true,
  "description": "在 pptx 演示文稿末尾批量追加幻灯片，每行一张：只填标题，或用 标题|正文 同时添加正文文本框。输出新文件不改原文件",
  "category": ["生成"],
  "inputFiles": {
    "min": 1,
    "max": 1
  },
  "accept": [".pptx"],
  "parameters": {
    "type": "object",
    "properties": {
      "inputFile": {
        "type": "file",
        "description": "目标 pptx 文件（新幻灯片追加到末尾）"
      },
      "slidesText": {
        "type": "string",
        "description": "要添加的幻灯片，每行一张；格式：标题 或 标题|正文（多个可用换行分隔）"
      },
      "output": {
        "type": "string",
        "output": true,
        "description": "输出 pptx 文件路径"
      }
    },
    "required": ["inputFile", "slidesText", "output"]
  }
}
